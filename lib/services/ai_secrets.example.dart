// Template for the Gemini demo API key.
//
// SETUP (each developer does this once):
//   1. Copy this file to `ai_secrets.dart` in the same folder.
//   2. Replace the empty string below with your Google Gemini API key
//      (get a free key at https://aistudio.google.com/apikey).
//
// `ai_secrets.dart` is gitignored, so your key is never committed.
// Do NOT put a real key in THIS file — it is committed to the repo.

const String geminiApiKey = '';
